---
title: screen-spec
description: 画面詳細設計書を記述する OpenAPI ライクな言語仕様（v0.1）。
---

# screen-spec

**screen-spec** は、画面詳細設計書を記述するための OpenAPI ライクな言語仕様です。
YAML を主な記述形式とし、JSON Schema で検証できます。特定の UI フレームワークに依存しません。

## 目的

- 人間と AI の双方が読み書きしやすい設計書フォーマット
- 画面・項目・状態・イベント・バリデーション・権限・API 連携・画面遷移を表現
- 実画面の自動生成ではなく、まず **設計書の記述と検証** を目指す（v0.1）
- 将来的な拡張と後方互換性を考慮

## v0.1 でできること

- `screen` と `components` の記述（YAML）
- `$ref` によるコンポーネント再利用（**純粋参照**、OpenAPI 互換）
- JSON Schema（draft 2020-12）＋リゾルバによる **2 段検証**
- `screen-spec validate <file>` CLI

## まず読む

- [はじめる](/getting-started) — インストールと検証コマンド
- [ドキュメントの種類](/concepts/documents)
- [`$ref` ガイド](/concepts/refs)
- [状態遷移](/concepts/state-machine) — states / events / transitions
- [API 連携](/concepts/api-bindings) — apiBindings（OpenAPI 参照）
- [パラメータ](/reference/params) — path / query の宣言
- [レイアウト](/reference/layout) — 言語レベルの構造（セクション/列/幅）
- [デザイン参照](/reference/design) — Figma / 画像 / リンク
- [テストデータ](/reference/test-data) — フィクスチャ（別ドキュメント種別）
- [テスト項目生成](/reference/test-generation) — 仕様からテスト候補を自動導出
- [要素リファレンス: Screen](/reference/screen)

## ビューア

設計書をブラウザで閲覧できる [ビューア](/screen-spec/viewer/)（Swagger UI 型）。
spec を fetch し、その場で `$ref` を解決して表示します。複数画面の一覧、各画面の項目・
状態遷移図・API 連携、そして**画面間遷移図**を表示します。

## 実装済み・未実装

- ✅ `screen` / `fields` / `validations` / `options` / `permissions`（M1）
- ✅ `$ref` コンポーネント再利用・2 段検証（M1）
- ✅ Viewer（Swagger UI 型・M2）
- ✅ Field Review Workspace（デザイン併置・Field詳細・関係追跡）
- ✅ `states` / `events` / `transitions`（状態遷移・M3）
- ✅ `apiBindings`（OpenAPI 連携・M4）
- ✅ 権限マトリクス、field既定値・`enabledWhen`、テストデータ
- ✅ 画面仕様・testDataからのテスト項目候補の自動導出とViewer表示
- ✅ テスト項目のMarkdown/CSV出力（core・CLI・Viewer）
- ✅ `compose`（allOf 相当の明示合成）
- ✅ 複数画面・testDataの横断検証（core・CLI・Viewer）
