# Agent Working Agreement

このリポジトリで作業するAgentは、変更の速さよりも、理解可能で検証可能な小さな変更を積み重ねることを優先する。

## Commit policy

Agentが作成するコミットは、常にConventional Commitsに従い、コミットメッセージを正確に3行で記述する。

```text
type(scope): concise summary

変更の意図、理由、または利用者への効果を説明する本文。
```

- 1行目は`type(scope): summary`形式とし、`feat`、`fix`、`refactor`、`test`、`docs`、`chore`など適切なtypeを選ぶ。
- 2行目は必ず空行にする。
- 3行目は「何をしたか」の繰り返しではなく、「なぜ必要か」または「どの振る舞いを保証するか」を書く。
- Agentが一度に作るコミットは、単一の論理的変更に限定する。

## Test-driven development

Kent BeckのTDDに基づき、振る舞いを変更するときはRed-Green-Refactorを基本サイクルとする。

1. 期待する振る舞いを表す、失敗する最小のテストを先に追加する。
2. テストが意図した理由で失敗することを確認する。
3. テストを通す最小の実装を行う。
4. 全テストが通る状態を保ちながら、重複・命名・責務を整理する。

- バグ修正では、報告された不具合を再現する回帰テストを実装より先に追加する。
- テストを通すために、既存の期待値を弱めたり、意味のある検証を削除したりしない。
- UI変更では、純粋関数・ViewModel・アクセシビリティ属性など自動検証可能な境界を優先してテストする。
- 自動テストが現実的でない場合は、その理由を明示し、型検査・ビルド・手動確認など代替検証を行う。
- テストファーストが不可能だった場合は隠さず、実装直後にcharacterization testまたは回帰テストを追加する。

## Domain-driven design

Eric EvansのDDDを、ユビキタス言語、境界、不変条件、モデルを中心に適用する。戦術的パターンを目的なく導入しない。

- 設計と実装の前に`CONTEXT.md`を読み、コード・スキーマ・UI・テストで同じドメイン用語を使う。
- 新しい概念や意味の変更が生じたら、実装と同じ変更内で`CONTEXT.md`を更新する。
- 曖昧な言葉をそのまま型名やプロパティ名にせず、具体的なシナリオと反例で意味を明確にする。
- ドメインの不変条件は、UIの条件分岐ではなく、可能な限りcoreのモデル、検証、型、テストで表現する。
- Viewer、Editor、CLIはドメインモデルの利用者とし、表示都合の概念をcoreの言語へ混入させない。
- Authored Specification、Resolved Specification、Viewer ViewModelの境界を保ち、同じデータに異なる責務を持たせない。
- Aggregate、Entity、Value Object、Repositoryなどは、対象のライフサイクルと整合性境界が必要な場合にだけ導入する。
- 重要な設計判断は、将来の読者が理由を必要とし、変更コストと実際のトレードオフがある場合に限ってADRへ記録する。

## Completion criteria

- 変更に対応するテストがあり、意図した振る舞いを説明している。
- 型検査、関連テスト、必要なビルドが成功している。
- 用語、スキーマ、ドキュメント、サンプルが変更後のモデルと矛盾していない。
- コミットする場合は、上記の3行Conventional Commit形式を満たしている。
